#include <stdio.h>

int main() {
    int newList[] = {29, 39, 459, 289, 234849};
    int length = sizeof(newList) / sizeof(newList[0]);

    printf("%d", length);  // Prints 5
    newList[0] = 1;
    for (i = 0; i < 4; i++) {
        printf("%d\n", myNumbers[i]);
        }
    char jk[] = {'h', 'e', 'l', 'l', 'o', '\0'}; // the \0 thing is the end of the string
    char je = "hello there"; // can also do this


    
}