#include <stdio.h>
// header file library, std = standard library, io = in/out, .h = header

int main() {                //main is the only function that will be run automatically by c
    printf("Hello World!");
    return 0;
}

//don't need whitespace